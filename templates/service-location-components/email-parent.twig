{% if serviceLocationChannel.email %}
<h3>Sähköposti</h3>
<article data-type="kunta-api-service-location-component" data-component="email" data-service-id="{{ serviceId }}" data-service-channel-id="{{ serviceLocationChannel.id }}" data-lang="{{ lang }}">{% include 'service-location-components/email.twig' %}</article>
{% endif %}